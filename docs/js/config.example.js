// Скопируйте в config.js или задайте API_PUBLIC_URL в GitHub → Settings → Secrets and variables → Actions
window.PORTAL_CONFIG = {
  // Публичный URL вашего API (обязательно HTTPS в продакшене)
  apiBase: 'https://api.example.com'
};
